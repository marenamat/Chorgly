recorded_at: '2026-04-18'
branch: issue-2
status: in progress — CI, tests, smarter blocked check implemented
summary: >
  Session 3 changes (issue #2 prototype continuation):

  Q1 resolved: Android = PWA (already documented in design doc since session 2).
  questions.md cleared — no open questions remain.

  Smarter client-side blocked check:
  - Added is_chore_blocked(&self, chore_id: &str) -> bool to AppState (state.rs).
    Uses Chore::is_blocked() against the full chore+event HashMap — correctly
    handles deps that are already satisfied (old code just checked list length > 0).
  - Updated app.js to call state.is_chore_blocked(c.id) instead of the length check.

  CI build workflow (.github/workflows/build.yml):
  - `native` job: cargo check --workspace --exclude chorgly-frontend + cargo test -p chorgly-core.
    Installs pkg-config + libssl-dev to satisfy git2 / openssl-sys.
  - `wasm` job: wasm-pack build --target web for chorgly-frontend, outputs to docs/pkg/.
  - Both jobs cache the cargo registry for fast re-runs.

  Integration tests (src/core/src/chore.rs #[cfg(test)]):
  - 11 tests covering: OneTime/RecurringAfterCompletion next_due logic,
    is_blocked with chore deps and event deps (both blocked and unblocked cases),
    visible_to_user, completable_by.

notes:
  - docs/pkg/ (wasm-pack output) is generated by CI; not committed to git.
  - The wasm-pack build.yml job will produce docs/pkg/ as a build artifact.
    The pages.yml workflow currently deploys docs/ as-is; a future task should
    chain build→deploy so the WASM output is included in the deployment.
next_steps:
  - Guardian reviews and merges issue-2
  - Chain build→deploy in CI (wasm-pack output → docs/pkg → pages deploy)
  - Proper cron schedule parsing for RecurringScheduled kind
  - Smarter server-side RecurringScheduled next_due (replace placeholder `Some(now)`)
